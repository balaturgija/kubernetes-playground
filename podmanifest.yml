apiVersion: v1
kind: Pod
metadata:
  name: nginx
spec:
  containers:
  - name: nginx
    image: nginx:1.14.2
    resources:
      requests:
        cpu: 250m
        memory: "65m"
      limits: 
        cpu: 500m
        memory: "130m"
